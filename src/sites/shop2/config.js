/**
 * Shop 2 – Custom layout & styles. Same products as shop1 (shared product list).
 */
import { products as shop1Products } from '../shop1/config.js';

export const name = 'Clean Haven';
export const slug = 'shop2';
export const imageBasePath = '/shop1';

export const products = shop1Products;
